# Sanitized Network Inventory

- Authorized managed-preview requests: 59.
- Foreign hosted-project requests: 0.
- Production-host requests: 0.
- Anonymous ordinary `games`/`events` table reads: 0.
- Anonymous share reads used only the public-safe Live Share RPC.

The request-level sanitized inventory is in `managed-preview-network-inventory.json`. The authorized host is represented as `authorized-preview.supabase.co`; no project reference, key, token, email, note, or tag is stored.
